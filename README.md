
# 🚀 XRAY VPN + TELEGRAM BOT v23.0-full
XRAY VPN + Telegram Bot - Full-featured VPN server with Reality masking

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Version](https://img.shields.io/badge/version-v23.0--full-brightgreen.svg)
![Language](https://img.shields.io/badge/language-Bash%2FPython-orange.svg)
![Status](https://img.shields.io/badge/status-Stable-success.svg)

Полнофункциональный VPN сервер на базе ** ядра Xray** с **Reality маскировкой** и управлением через **Telegram Bot**.
скрипт подходит для личного, некоммерческого использования!

📝INSTALLATION.md - инструкция по установке
⚙️XRAY-Admin-Guide.md - инструкция по администрированию 
🔒install-v23.0-full-fixed.sh -сам скрипт 

## ✨ Основные возможности
- 🔒 **VPN сервер** - VLESS протокол с Reality маскировкой (выглядит как HTTPS к GitHub/Google/Yahoo)
- 🤖 **Telegram управление** - полное управление из мобильного приложения
- 🔔 **Надежные уведомления** - информирование в Telegram о смене Reality, перезагрузке сервера
- 👥 **Управление пользователями** - добавление/удаление пользователей прямо из Telegram
- 🎭 **Белый список маскировок** - GitHub, Google, Yahoo (с возможностью смены "в горячем" режиме)
- 📊 **Полная диагностика** - проверка здоровья всей системы одной командой
- ⚙️ **systemd сервисы** - автоматический запуск, перезапуск при сбое
- 💾 **Резервные копии** - автоматическое сохранение конфигов
- 📝 **Режим обновления** - сохранение всех параметров при переустановке
- 🌐 внимание! могут быть ошибки проект делался с применением подхода вайб-кодинга!( на серверах aeza не всегда работает, в остальном работает отлично ) 

## 🔧 Требования
- **ОС**: Ubuntu 18.04+ / Debian 10+
- **Права**: Root доступ
- **Портсы**: 443/TCP (открыт и доступен)
- **Интернет**: Доступ к GitHub, Google или Yahoo
- **Telegram**: Токен бота (@BotFather) + User ID (@userinfobot)

## ⚡ Быстрая установка
### 1. Скачиваем скрипт

```bash
wget https://raw.githubusercontent.com/yuriyK0/xray-telegram/main/install-v23.0-full-fixed.sh

```
chmod +x install-v23.0-full-fixed.sh


### 2. Подготавливаем данные
Перед запуском вам понадобятся:
- **IP сервера** (или Enter для автоопределения)
- **Telegram Bot Token** - получить от @BotFather
- **Telegram User ID** - получить от @userinfobot
- **Reality маскировка** - автоматически определится (GitHub, Google или Yahoo)

### 3. Запускаем установку

```bash
sudo bash install-v23.0-full-FIXED.sh
```

### 4. Вводим данные

Скрипт попросит интерактивно:
```
🌐 IP адрес сервера (Enter для автоопределения): [автоматически определиться]
🤖 Токен Telegram бота (@BotFather): 123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ...
👤 Ваш Telegram User ID (@userinfobot): 123456789
🎭 Автоопределение Reality... [GitHub/Google/Yahoo]
```

### 5. Подтверждаем установку

```
🚀 Начать установку v23.0-full? (y/n): y
```

И готово! Система автоматически:
- ✅ Установит все зависимости
- ✅ Скачает и установит Xray
- ✅ Сгенерирует ключи (UUID, x25519, SHORT_ID)
- ✅ Создаст systemd сервисы
- ✅ Запустит все сервисы
- ✅ Отправит первое уведомление в Telegram

## 📱 Использование Telegram Bot

После установки бот появится в Telegram и покажет меню:

```
🤖 Xray Management Bot v23.0-full

Выберите действие:
➕ Создать пользователя    ➖ Удалить пользователя
📜 Список пользователей    📊 Статус сервера
🔄 Перезапуск Xray         🎭 Смена маскировки
🔄 Перезагрузка сервера    📖 Руководство
🔧 Диагностика             📋 Логи
```

### Примеры команд:

**Добавить пользователя:**
1. Нажимаем `➕ Создать пользователя`
2. Вводим имя пользователя: `alex`
3. Получаем готовую ссылку: `vless://...#alex`

**Смена маскировки:**
1. Нажимаем `🎭 Смена маскировки`
2. Выбираем домен: GitHub / Google / Yahoo
3. Подтверждаем
4. Получаем уведомление о успешной смене ✅

**Проверить статус:**
1. Нажимаем `📊 Статус сервера`
2. Видим: IP, статус Xray, Reality, Uptime, RAM

## 🖥️ Управление через SSH

### Базовые команды

```bash
# Статус сервисов
systemctl status xray
systemctl status xray_bot

# Перезапуск
systemctl restart xray
systemctl restart xray_bot

# Логи (в реальном времени)
journalctl -u xray -f
journalctl -u xray_bot -f

# Проверка портов
ss -tlnp | grep 443

# Полная диагностика
xray-diagnostics.sh
```

### Управление пользователями

```bash
# Добавить пользователя
newuser alex

# Список пользователей
listusers

# Удалить пользователя
deluser alex

# Смена маскировки
change-reality-domain.sh --force github.com

# Смена маскировки с подтверждением
change-reality-domain.sh google.com
```

### Логи и отладка

```bash
# Ошибки Xray
tail -f /var/log/xray/error.log

# Логи подключений
tail -f /var/log/xray/access.log

# Проверить конфиг
xray run -c /usr/local/etc/xray/config.json -test
```

## 📂 Структура файлов

```
/usr/local/etc/xray/
├── config.json           # основная конфигурация
├── .keys                 # ключи и параметры
├── bot_token.txt        # токен Telegram (root:root 600)
├── .chatid              # Chat ID владельца (root:root 600)
└── backups/             # резервные копии

/usr/local/bin/
├── xray                 # основной бинарь
├── xray_bot.py          # Telegram бот (Python3)
├── newuser              # добавление пользователя
├── listusers            # список пользователей
├── deluser              # удаление пользователя
├── change-reality-domain.sh    # смена маскировки
├── xray-diagnostics.sh         # диагностика
└── xray_notify_boot.sh         # уведомление о загрузке
```

## 🔒 Безопасность

- 🔐 **Права файлов**: `bot_token.txt` и `.chatid` имеют права `600` (только root)
- 🔑 **Аутентификация**: Только владелец (User ID) может управлять ботом
- 💾 **Резервные копии**: Автоматически создаются при смене Reality
- 📜 **Логирование**: Все события логируются в systemd journal и файлах логов

## 🚨 Решение проблем

### Бот не работает

```bash
systemctl status xray_bot
journalctl -u xray_bot -n 50
```

Проверьте что в файле `/usr/local/bin/xray_bot.py` корректные TOKEN и CHAT_ID.

### Уведомления не приходят

```bash
# Проверьте права:
ls -la /usr/local/etc/xray/bot_token.txt
ls -la /usr/local/etc/xray/.chatid

# Должно быть: -rw------- 1 root root
# Если не так, исправьте:
chown root:root /usr/local/etc/xray/bot_token.txt
chmod 600 /usr/local/etc/xray/bot_token.txt
chown root:root /usr/local/etc/xray/.chatid
chmod 600 /usr/local/etc/xray/.chatid
```

### Конфиг невалидный

```bash
# Тестируем конфиг:
xray run -c /usr/local/etc/xray/config.json -test

# Если ошибка - восстанавливаем из резервной копии:
cp /usr/local/etc/xray/backups/config.json.backup.* \
   /usr/local/etc/xray/config.json

systemctl restart xray
```

### Нет подключения

1. Проверьте открыт ли порт: `ss -tlnp | grep 443`
2. Проверьте статус: `systemctl status xray`
3. Посмотрите логи: `tail -f /var/log/xray/error.log`
4. Проверьте маскировку доступна: `curl -I https://github.com`

### Полная диагностика

```bash
xray-diagnostics.sh
```

Эта команда проверит ВСЁ:
- ✅ Статусы сервисов
- ✅ Открыт ли порт 443
- ✅ Наличие всех файлов конфига
- ✅ **ПРАВА на файлы (особенно bot_token.txt и .chatid)**
- ✅ Текущую маскировку Reality
- ✅ Количество пользователей

## 🗑️ Удаление

Чтобы полностью удалить всё:

```bash
sudo uninstall-xray.sh
```

Это удалит:
- ❌ Все сервисы systemd
- ❌ Все конфиги и ключи
- ❌ Все пользователей Xray
- ❌ Все логи

## 📊 Маскировки Reality (Whitelist)

Система поддерживает маскировку под эти сервисы:

| Домен | Использование | Скорость |
|-------|---|---|
| 🔵 GitHub | `github.com`, `www.github.com` | Быстро |
| 🔵 Google | `www.google.com`, `google.com` | Очень быстро |
| 🟣 Yahoo | `www.yahoo.com`, `yahoo.com` | Быстро |

После смены маскировки старые ссылки пользователей **перестанут работать**! Нужно создать новых пользователей.

## 📚 Документация

Для подробной документации смотрите:
- 📖 `XRAY-Admin-Guide.md` - полное руководство администратора
- ⚙️ `install-v23.0-full-FIXED.sh` - скрипт установки с комментариями


## 🙋 Поддержка
Если есть вопросы:
1. Проверьте `XRAY-Admin-Guide.md`
2. Запустите `xray-diagnostics.sh`
3. Посмотрите логи: `journalctl -u xray -n 100`

### 🚀 Начните с установки:

```bash
wget https://raw.githubusercontent.com/yuriyK0/xray-telegram/main/install-v23.0-full-fixed.sh
chmod +x install-v23.0-full-fixed.sh
sudo bash install-v23.0-full-fixed.sh
```

**Готово!** Система готова к использованию. Откройте Telegram и начните управлять VPN сервером! 🎉


