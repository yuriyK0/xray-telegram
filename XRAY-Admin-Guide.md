# XRAY VPN + TELEGRAM BOT v23.0-full - ПОЛНАЯ АДМИНСКАЯ ИНСТРУКЦИЯ

## 1. ОБЗОР ПРОЕКТА
- **Версия**: v23.0-full (FIXED)
- **Тип**: VPN сервер на базе Xray + Telegram управление
- **Протокол**: VLESS + Reality (маскировка под HTTPS)
- **Установка**: Автоматическая с интерактивной настройкой
- **Обновление**: Сохраняет старые ключи при обновлении

## 2. АРХИТЕКТУРА

### 2.1 ОСНОВНЫЕ КОМПОНЕНТЫ:
- **Xray Core** - VPN сервер (VLESS + Reality)
- **Telegram Bot** - управление пользователями и сервером
- **systemd сервисы** - автоматический запуск/перезапуск
- **Скрипты управления** - добавление/удаление пользователей, смена Reality
- **Boot notification** - уведомление в Telegram после перезагрузки

### 2.2 СЕРВИСЫ:
- `xray.service` - основной VPN сервер (порт 443/TCP)
- `xray_bot.service` - Telegram бот для управления
- `xray_notify_boot.service` - уведомление о загрузке (oneshot)

### 2.3 ЛОГИКА РАБОТЫ:
1. Пользователь подключается к серверу (IP:443)
2. Трафик маскируется под HTTPS к GitHub/Google/Yahoo
3. Сервер распознает валидные VLESS клиенты
4. Остальной трафик передается на реальный сервер
5. Telegram бот позволяет управлять всем из мобильного телефона

## 3. КАТАЛОГИ И ФАЙЛЫ

### 3.1 ОСНОВНОЙ КАТАЛОГ: /usr/local/etc/xray/
```
├── config.json           - основная конфигурация Xray (JSON)
├── .keys                 - хранилище ключей и параметров (текст)
├── bot_token.txt        - токен Telegram бота (root:root 600) ✅ ВАЖНО
├── .chatid              - Chat ID владельца (root:root 600) ✅ ВАЖНО
└── backups/             - резервные копии конфигов
    ├── config.json.backup.1701525600
    └── .keys.backup.1701525600
```

### 3.2 БИНАРНЫЕ ФАЙЛЫ: /usr/local/bin/
```
├── xray                              - основной бинарь Xray
├── xray_bot.py                      - Telegram бот (Python3)
├── newuser                          - скрипт добавления пользователя
├── listusers                        - список пользователей
├── deluser                          - удаление пользователя
├── change-reality-domain.sh         - смена маскировки Reality
├── xray-diagnostics.sh              - диагностика системы
├── xray_notify_boot.sh              - уведомление о загрузке
└── uninstall-xray.sh                - удаление всего
```

### 3.3 ЛОГИ: /var/log/xray/
```
├── access.log    - логи подключений
└── error.log     - логи ошибок
```

### 3.4 СЕРВИСЫ: /etc/systemd/system/
```
├── xray.service
├── xray_bot.service
└── xray_notify_boot.service
```

## 4. КОНФИГУРАЦИОННЫЕ ФАЙЛЫ

### 4.1 /usr/local/etc/xray/config.json (JSON)
Структура основного конфига:
```json
{
  "log": {                    // логирование
    "access": "/var/log/xray/access.log",
    "error": "/var/log/xray/error.log",
    "loglevel": "warning"
  },
  "inbounds": [               // входящие подключения
    {
      "port": 443,            // порт слушания
      "protocol": "vless",    // протокол VLESS (без шифрования)
      "settings": {
        "clients": [          // массив пользователей
          {
            "id": "UUID",     // уникальный ID пользователя
            "flow": "xtls-rprx-vision",  // тип потока
            "email": "username"  // имя пользователя
          }
        ],
        "decryption": "none"  // без дополнительного шифрования
      },
      "streamSettings": {
        "network": "tcp",     // протокол транспорта
        "security": "reality", // маскировка Reality
        "realitySettings": {
          "show": false,
          "dest": "github.com:443",     // маскировка под этот домен
          "xver": 0,
          "serverNames": ["github.com", "www.github.com"],
          "privateKey": "...",   // приватный ключ x25519
          "shortIds": ["12345678..."]  // короткий ID
        }
      },
      "sniffing": {           // автоопределение протокола
        "enabled": true,
        "destOverride": ["http", "tls"]
      }
    }
  ],
  "outbounds": [              // исходящие подключения
    {
      "protocol": "freedom",  // прямой выход в интернет
      "tag": "direct"
    }
  ],
  "routing": {                // маршрутизация
    "domainStrategy": "IPIfNonMatch",
    "rules": []
  }
}
```

### 4.2 /usr/local/etc/xray/.keys (текстовый формат)
Содержит критичные параметры:
```
uuid: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
Private key: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx (x25519)
Public key: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
shortsid: 1234567890abcdef
reality_dest: github.com:443
reality_sni: github.com
reality_name: GitHub
```

### 4.3 /usr/local/etc/xray/bot_token.txt (root:root 600) ✅
Содержит только токен Telegram бота:
```
1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh
```
**Важно**: принадлежит root, права 600 - для безопасности

### 4.4 /usr/local/etc/xray/.chatid (root:root 600) ✅
Содержит только Chat ID владельца:
```
123456789
```
**Важно**: принадлежит root, права 600 - для безопасности

## 5. ПРАВА ДОСТУПА

### 5.1 ПРАВИЛЬНАЯ СТРУКТУРА ПРАВ:
```
/usr/local/etc/xray/
├── config.json       (xray:xray, 600)   - конфиг от xray
├── .keys            (xray:xray, 644)   - ключи от xray (читаемо)
├── bot_token.txt    (root:root, 600)   - токен от root ✅
├── .chatid          (root:root, 600)   - chat_id от root ✅
└── backups/         (xray:xray, 755)   - папка от xray

/var/log/xray/
├── access.log       (xray:xray, 644)
└── error.log        (xray:xray, 644)

/usr/local/bin/
├── xray              (755)  - выполняемый
├── xray_bot.py       (755)  - выполняемый
├── newuser           (755)  - выполняемый
└── ...
```

### 5.2 ПРОВЕРКА ПРАВ:
```bash
ls -la /usr/local/etc/xray/
ls -la /usr/local/etc/xray/bot_token.txt      # root root 600
ls -la /usr/local/etc/xray/.chatid            # root root 600
```

### 5.3 ИСПРАВЛЕНИЕ ПРАВ (если сломались):
```bash
chown root:root /usr/local/etc/xray/bot_token.txt
chmod 600 /usr/local/etc/xray/bot_token.txt

chown root:root /usr/local/etc/xray/.chatid
chmod 600 /usr/local/etc/xray/.chatid
```

## 6. СКРИПТЫ И ФУНКЦИИ

### 6.1 УПРАВЛЕНИЕ ПОЛЬЗОВАТЕЛЯМИ

**newuser <имя>**
- Добавляет нового пользователя VPN
- Генерирует UUID
- Создает подходящую ссылку (vless://)
- Перезапускает Xray

$ newuser alex
✅ Created: alex
vless://uuid@1.2.3.4:443?encryption=none&flow=...#alex
```

**listusers**
- Показывает всех пользователей
- Показывает количество

$ listusers
Users:
• main
• alex
• bob
Total: 3
```

**deluser <имя>**
- Удаляет пользователя
- Перезапускает Xray

$ deluser alex
✅ Deleted: alex
```

### 6.2 СМЕНА REALITY (маскировки)

**change-reality-domain.sh <домен>**
- Интерактивная смена маскировки
- Запрашивает подтверждение
- Содержит whitelist (GitHub, Google, Yahoo)
- Делает резервную копию конфига
- Проверяет валидность конфига
- Перезапускает Xray

**change-reality-domain.sh --force <домен>**
- Автоматическая смена БЕЗ подтверждения
- Отправляет уведомление через Telegram API
- Полезна для автоматизации

### 6.3 ДИАГНОСТИКА

**xray-diagnostics.sh**
- Проверяет статус всех сервисов (Xray, Bot, Boot notify)
- Проверяет открытый порт 443
- Проверяет наличие всех конфиг файлов
- **Проверяет права на файлах (особенно bot_token.txt и .chatid)**
- Показывает текущую маскировку Reality
- Показывает количество пользователей

## 7. TELEGRAM БОТ

### 7.1 ОСНОВНЫЕ ФУНКЦИИ БОТА:

Главное меню (4 колонки кнопок):
1. ➕ Создать пользователя (Add User)
2. ➖ Удалить пользователя (Delete User)
3. 📜 Список пользователей (User List)
4. 📊 Статус сервера (Server Status)
5. 🔄 Перезапуск Xray (Restart Xray)
6. 🎭 Смена маскировки (Change Reality)
7. 🔄 Перезагрузка сервера (Reboot Server)
8. 📖 Руководство (Guide)
9. 🔧 Диагностика (Diagnostics) - показывает права!
10. 📋 Логи (Logs)

### 7.2 АВТОРИЗАЦИЯ:
- Только владелец (OWNER_ID) может использовать бота
- Проверка USER_ID при каждой команде
- Отклонение неавторизованных пользователей

### 7.3 КОД БОТА: /usr/local/bin/xray_bot.py
- Написан на Python3 с библиотекой pyTelegramBotAPI
- Работает как systemd сервис (xray_bot.service)
- Автоматически перезапускается при ошибке
- Логирует все события

## 8. УВЕДОМЛЕНИЯ (ИСПРАВЛЕНО в v23.0-full)

### 8.1 АРХИТЕКТУРА УВЕДОМЛЕНИЙ:

**Тип 1: Перезагрузка Xray**
- Откуда: Telegram бот через кнопку "Перезапуск Xray"
- Как: Прямое сообщение в Telegram
- Адрес: Chat ID из `/usr/local/etc/xray/.chatid`
- Статус: ✅ Работает

**Тип 2: Смена Reality**
- Откуда: Скрипт `change-reality-domain.sh --force`
- Как: Прямой API запрос к Telegram
- Метод: curl POST `https://api.telegram.org/bot{TOKEN}/sendMessage`
- Адрес: Chat ID из `/usr/local/etc/xray/.chatid`
- Токен: `/usr/local/etc/xray/bot_token.txt` (root:root 600) ✅
- Статус: ✅ Работает (ИСПРАВЛЕНО)

**Тип 3: Загрузка сервера**
- Откуда: systemd сервис `xray_notify_boot.service`
- Как: Скрипт `xray_notify_boot.sh`
- Метод: curl POST (с 3 попытками)
- Адрес: Chat ID из `/usr/local/etc/xray/.chatid`
- Токен: `/usr/local/etc/xray/bot_token.txt` (root:root 600) ✅
- Статус: ✅ Работает (ИСПРАВЛЕНО)

### 8.2 ИСПРАВЛЕНИЯ В v23.0-full:

**БЫЛО (ломалось):**
- bot_token.txt и .chatid принадлежали xray:xray
- Права были 644 (читаемо для всех)
- Скрипты запускались от xray, не могли читать файлы root
- Уведомления не отправлялись

**СТАЛО (v23.0-full):**
- bot_token.txt и .chatid принадлежат root:root
- Права 600 (только root может читать)
- Скрипты запускаются от root
- Уведомления работают надежно ✅

### 8.3 ПРОВЕРКА РАБОТЫ УВЕДОМЛЕНИЙ:

```bash
# Проверить файлы:
$ ls -la /usr/local/etc/xray/bot_token.txt
-rw------- 1 root root 104 Jan 2 09:00 /usr/local/etc/xray/bot_token.txt

$ ls -la /usr/local/etc/xray/.chatid
-rw------- 1 root root 10 Jan 2 09:00 /usr/local/etc/xray/.chatid

# Проверить содержимое:
$ cat /usr/local/etc/xray/bot_token.txt      # токен Telegram бота
$ cat /usr/local/etc/xray/.chatid            # Chat ID (число)

# Тест диагностики:
$ xray-diagnostics.sh | grep -A5 "NOTIFICATION FILES"

# Вручную отправить уведомление:
$ TOKEN=$(cat /usr/local/etc/xray/bot_token.txt)
$ CHAT_ID=$(cat /usr/local/etc/xray/.chatid)
$ curl -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
  -d chat_id="$CHAT_ID" \
  -d text="Test" \
  -d parse_mode="Markdown"
```

## 9. УПРАВЛЕНИЕ VIA SSH

### 9.1 ОСНОВНЫЕ КОМАНДЫ:

```bash
# Статус:
$ systemctl status xray
$ systemctl status xray_bot
$ systemctl status xray_notify_boot

# Перезапуск:
$ systemctl restart xray
$ systemctl restart xray_bot

# Логи:
$ journalctl -u xray -f              # realtime логи Xray
$ journalctl -u xray_bot -f          # realtime логи бота
$ journalctl -u xray_notify_boot -f  # логи загрузки
$ tail -f /var/log/xray/error.log    # ошибки

# Проверка портов:
$ ss -tlnp | grep 443

# Тестирование конфига:
$ /usr/local/bin/xray run -c /usr/local/etc/xray/config.json -test

# Управление пользователями:
$ /usr/local/bin/newuser username       # добавить
$ /usr/local/bin/listusers              # список
$ /usr/local/bin/deluser username       # удалить

# Смена Reality:
$ /usr/local/bin/change-reality-domain.sh github.com          # интерактивно
$ /usr/local/bin/change-reality-domain.sh --force google.com  # с уведомлением

# Диагностика:
$ /usr/local/bin/xray-diagnostics.sh
```

## 10. ОТЛАДКА И РЕШЕНИЕ ПРОБЛЕМ

### 10.1 Проблема: Бот не работает
```bash
$ systemctl status xray_bot
$ journalctl -u xray_bot -n 50
# Проверить: TOKEN и CHAT_ID в /usr/local/bin/xray_bot.py
```

### 10.2 Проблема: Уведомления не приходят
```bash
# Должны быть права 600, owner root:root
$ ls -la /usr/local/etc/xray/bot_token.txt
$ cat /usr/local/etc/xray/bot_token.txt       # не должно быть пусто
$ cat /usr/local/etc/xray/.chatid             # должен быть числовой ID

# Если права неправильные:
$ chown root:root /usr/local/etc/xray/bot_token.txt
$ chmod 600 /usr/local/etc/xray/bot_token.txt
$ chown root:root /usr/local/etc/xray/.chatid
$ chmod 600 /usr/local/etc/xray/.chatid
```

### 10.3 Проблема: Конфиг невалидный
```bash
$ /usr/local/bin/xray run -c /usr/local/etc/xray/config.json -test
# Восстановить из бэкапа:
$ cp /usr/local/etc/xray/backups/config.json.backup.* /usr/local/etc/xray/config.json
```

### 10.4 ПОЛНАЯ ДИАГНОСТИКА:
```bash
$ /usr/local/bin/xray-diagnostics.sh
# Проверяет ВСЁ:
# - Статусы сервисов
# - Открыт ли порт
# - Наличие всех файлов
# - ПРАВА на файлы (особенно bot_token.txt и .chatid) ✅
# - Текущую маскировку
# - Количество пользователей
```

## 11. WHITELIST МАСКИРОВОК (v23.0-full)

### 11.1 ПОДДЕРЖИВАЕМЫЕ ДОМЕНЫ:
- GitHub: `github.com`, `www.github.com`
- Google: `www.google.com`, `google.com`  
- Yahoo: `www.yahoo.com`, `yahoo.com`

### 11.2 ОПИСАНИЕ:
Трафик маскируется под HTTPS подключение к этим сервисам. Системный администратор не может разглядеть настоящий трафик. Для системного администратора это выглядит как обычный просмотр GitHub/Google/Yahoo.

### 11.3 ПЕРЕХОД НА ДРУГОЙ ДОМЕН:
```bash
$ change-reality-domain.sh --force github.com
```
или через бота: 🎭 Смена маскировки (Change Reality)

**ВАЖНО**: После смены Reality старые ссылки пользователей перестанут работать! Необходимо создать новых пользователей и раздать новые ссылки.

## 12. БЕЗОПАСНОСТЬ

### 12.1 ПРАВА ФАЙЛОВ:
- `config.json`: 600 (только xray читает)
- `bot_token.txt`: 600 (только root читает) ✅ КРИТИЧНО
- `.chatid`: 600 (только root читает) ✅ КРИТИЧНО

### 12.2 АУТЕНТИФИКАЦИЯ:
- Telegram: только OWNER_ID может управлять ботом
- SSH: стандартные права root
- Конфиг: JSON зашифрован правами файловой системы

### 12.3 ЛОГИРОВАНИЕ:
- Access log: все подключения
- Error log: все ошибки
- Journal: все события systemd

### 12.4 РЕЗЕРВНЫЕ КОПИИ:
Автоматически создаются в `/usr/local/etc/xray/backups/`:
- `config.json.backup.{timestamp}`
- `.keys.backup.{timestamp}`

## 13. ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ (FAQ)

**Q: Почему уведомления не работали раньше?**
A: Потому что bot_token.txt и .chatid принадлежали xray:xray, а скрипты запускались от root. В v23.0-full это исправлено - файлы принадлежат root:root (права 600)

**Q: Как узнать Chat ID для Telegram?**
A: Найти бота @userinfobot, нажать /start, бот покажет ID

**Q: Можно ли менять Reality без перезагрузки?**
A: Да! `change-reality-domain.sh` меняет его "в горячем" режиме, старые клиенты переподключаются

**Q: Что будет со старыми пользователями при смене Reality?**
A: Их ссылки (URLs) перестанут работать, нужно создать новых пользователей с новыми ссылками

**Q: Мой Telegram бот не работает**
A: Проверьте: TOKEN в bot_token.txt, CHAT_ID в .chatid, права доступа (600, owner root)

**Q: Как удалить всё и переустановить?**
A: `sudo /usr/local/bin/uninstall-xray.sh` (удалит ВСЁ), потом скрипт установки заново

## 14. ПОЛЕЗНЫЕ КОМАНДЫ БЫСТРОГО ДОСТУПА

```bash
# Быстрая полная проверка:
$ xray-diagnostics.sh && echo "OK" || echo "FAIL"

# Посмотреть текущую маскировку:
$ grep reality_name /usr/local/etc/xray/.keys

# Проверить количество пользователей:
$ jq '.inbounds[0].settings.clients | length' /usr/local/etc/xray/config.json

# Сгенерировать новый UUID для пользователя:
$ /usr/local/bin/xray uuid

# Проверить логи загрузки:
$ journalctl -u xray_notify_boot -n 50

# Выключить бота (если нужен перерыв):
$ systemctl stop xray_bot

# Включить бота обратно:
$ systemctl start xray_bot
```


