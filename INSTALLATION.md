# 📖 Инструкция по установке XRAY VPN + TELEGRAM BOT v23.0-full

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка](#подготовка)
3. [Пошаговая установка](#пошаговая-установка)
4. [Проверка установки](#проверка-установки)
5. [Первые шаги](#первые-шаги)
6. [Часто встречающиеся ошибки](#часто-встречающиеся-ошибки)

## Требования

### Минимальные требования к серверу:
- **ОС**: Ubuntu 18.04, 20.04, 22.04 или Debian 10, 11, 12
- **RAM**: 512 МБ (рекомендуется 1 ГБ+)
- **CPU**: 1 ядро (рекомендуется 2 ядра+)
- **Диск**: 2 ГБ свободного места
- **Сеть**: 
  - Открытый порт 443/TCP (входящий)
  - Доступ в интернет (исходящий)
  - Доступ к GitHub, Google или Yahoo

### Доступ в системе:
- ✅ Root доступ (sudo)
- ✅ SSH или консоль сервера
- ✅ curl установлен (обычно есть по умолчанию)

### Telegram:
- ✅ Активный Telegram аккаунт
- ✅ Токен бота (от @BotFather)
- ✅ Your User ID (от @userinfobot)

## Подготовка

### Шаг 1: Получить Telegram Bot Token

1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Нажимаем `/start`
4. Выбираем `/newbot`
5. Даем имя бота (например, `MyXrayBot`)
6. Даем username (например, `MyXrayBot_123`)
7. **@BotFather** отправит TOKEN вида: `123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ...`

⚠️ **ВАЖНО**: Сохраните этот токен! Больше его не покажет.

### Шаг 2: Получить Telegram User ID

1. Найдите бота **@userinfobot**
2. Нажимаем `/start`
3. Бот покажет ваш User ID (число типа `123456789`)

⚠️ **ВАЖНО**: Это число нужно для управления ботом. Только вы сможете управлять своим VPN!

### Шаг 3: Проверить открыт ли порт 443

```bash
# На вашем сервере проверьте:
netstat -tlnp | grep 443
# или
ss -tlnp | grep 443

# Если видите что-то типа:
# LISTEN 0 128 0.0.0.0:443 0.0.0.0:* 
# Это значит порт ЗАНЯТ! Нужно освободить.

# Проверьте что порт не блокируется firewall'ом:
sudo ufw status
# Если iptables, то проверьте:
sudo iptables -L -n | grep 443
```

### Шаг 4: Определить IP сервера

Скрипт автоматически определит IP, но можно проверить вручную:

```bash
# Способ 1 (для публичного IP):
curl -s icanhazip.com
# или
curl -s ifconfig.me

# Способ 2 (локальный IP):
hostname -I
```

## Пошаговая установка

### Вариант 1: Установка через одну команду

```bash
# Скачиваем скрипт и запускаем сразу:
curl -O https://raw.githubusercontent.com/your-username/xray-telegram/main/install-v23.0-full-FIXED.sh && \
chmod +x install-v23.0-full-FIXED.sh && \
sudo bash install-v23.0-full-FIXED.sh
```

### Вариант 2: Пошагово

```bash
# Шаг 1: Скачиваем
wget https://raw.githubusercontent.com/your-username/xray-telegram/main/install-v23.0-full-FIXED.sh

# Шаг 2: Даем права на выполнение
chmod +x install-v23.0-full-FIXED.sh

# Шаг 3: Запускаем от root
sudo bash install-v23.0-full-FIXED.sh
```

### Что происходит при установке

Скрипт интерактивно попросит ввести:

```
╔════════════════════════════════════════════════════════════════╗
║ XRAY VPN + TELEGRAM BOT                                        ║
║ v23.0-full                                                     ║
║ 🔔 НАДЁЖНЫЕ уведомления (API + systemd service)              ║
║ 🔧 ИСПРАВЛЕНА смена Reality из бота (--force режим)          ║
║ 🎭 WHITELIST: GitHub, Google, Yahoo                          ║
║ ✅ ИСПРАВЛЕНЫ права файлов для работы из коробки              ║
╚════════════════════════════════════════════════════════════════╝

📋 Интерактивная настройка v23.0-full

🌐 IP адрес сервера (Enter для автоопределения): 
# Нажимаете Enter - система определит IP автоматически
# Или вводите вручную: 1.2.3.4

🤖 Токен Telegram бота (@BotFather): 
# Вставляете токен из @BotFather (Ctrl+Shift+V в PuTTY)
# Например: 123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh

👤 Ваш Telegram User ID (@userinfobot): 
# Вводите ваш User ID (число)
# Например: 123456789

🎭 Автоопределение Reality...
# Система сама проверит доступность и выберет лучшую маскировку
✅ Выбрана маскировка: GitHub

✅ Настройка завершена:
 🌐 IP: 1.2.3.4
 🤖 Токен: 123456789:A...
 👤 Owner: 123456789
 🎭 Reality: GitHub

🚀 Начать установку v23.0-full? (y/n): y
# Вводите "y" и Enter - начнется установка
```

### Автоматическая установка

После подтверждения скрипт:

1. 🧹 **Очистка** - удалит старые конфиги (если есть)
2. 📦 **Зависимости** - установит curl, jq, python3, etc
3. 📥 **Xray** - скачает и установит последнюю версию
4. 🔐 **Ключи** - сгенерирует UUID, x25519, SHORT_ID
5. ⚙️ **Конфиги** - создаст config.json и .keys
6. 👤 **Пользователь** - создаст системного пользователя xray
7. 🔧 **Сервисы** - создаст systemd сервисы
8. ✅ **Запуск** - запустит все сервисы
9. 🔔 **Уведомление** - отправит первое сообщение в Telegram

Все это занимает обычно **2-3 минуты**.

## Проверка установки

### Вариант 1: Через встроенную диагностику

```bash
# Полная проверка системы:
xray-diagnostics.sh
```

Должно показать:

```
🔧 SERVICES:
 Xray: ✅ Active
 Bot: ✅ Active
 Boot Notify (oneshot - normal if inactive): ✅ Inactive

🌐 PORTS:
 Port 443: ✅ Open

📁 FILES:
 config.json: ✅ Exists
 .keys: ✅ Exists
 .chatid: ✅ Exists

🔔 NOTIFICATION FILES:
 bot_token.txt: ✅ Exists (104 chars)
 Owner: root:root      ✅ ПРАВИЛЬНО!
 .chatid: ✅ Exists (123456789)
 Owner: root:root      ✅ ПРАВИЛЬНО!

🎭 REALITY:
 Masking: github.com:443

📊 USERS:
 Total: 1
```

### Вариант 2: Проверки по отдельности

```bash
# Статус Xray:
systemctl status xray
# Должно быть: active (running) ✅

# Статус Telegram бота:
systemctl status xray_bot
# Должно быть: active (running) ✅

# Проверить слушает ли Xray порт 443:
ss -tlnp | grep 443
# Должно быть: LISTEN 0 128 0.0.0.0:443

# Проверить количество пользователей:
jq '.inbounds[0].settings.clients | length' /usr/local/etc/xray/config.json
# Должно быть: 1

# Проверить текущую маскировку:
grep reality_name /usr/local/etc/xray/.keys
# Должно быть: reality_name: GitHub
```

## Первые шаги

### Шаг 1: Проверить бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота (по username которому вы дали)
3. Нажимаем `/start`
4. Бот должен ответить с меню кнопок

### Шаг 2: Добавить первого пользователя

**Через Telegram:**
1. Нажимаем `➕ Создать пользователя`
2. Вводим имя: `alex`
3. Получаем ссылку: `vless://...#alex`

**Или через SSH:**
```bash
newuser alex
```

Получите ссылку вида:
```
vless://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx@1.2.3.4:443?\
encryption=none&flow=xtls-rprx-vision&security=reality&sni=github.com&\
fp=chrome&pbk=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx&sid=xxxxxxxx&type=tcp#alex
```

### Шаг 3: Подключиться к VPN

Используйте ссылку в VPN клиентах:
- iOS: v2rayNG, Shadowrocket, Quantumult
- Android: v2rayNG, Shadowrocket
- Windows: v2rayN, Clash, Sing-box
- macOS: v2rayU, Clash, Sing-box

### Шаг 4: Проверить подключение

```bash
# На сервере смотрим логи:
tail -f /var/log/xray/access.log

# Если видите строку с вашим UUID - подключение работает! ✅
```

## Часто встречающиеся ошибки

### Ошибка 1: "Permission denied"

**Проблема:**
```bash
bash: install-v23.0-full-FIXED.sh: Permission denied
```

**Решение:**
```bash
chmod +x install-v23.0-full-FIXED.sh
# или
sudo bash install-v23.0-full-FIXED.sh
```

### Ошибка 2: "Требуются права root"

**Проблема:**
```
❌ Требуются права root
```

**Решение:**
```bash
# Используйте sudo:
sudo bash install-v23.0-full-FIXED.sh

# Или станьте root:
su -
bash install-v23.0-full-FIXED.sh
```

### Ошибка 3: "Конфигурация невалидна"

**Проблема:**
```
❌ Ошибка конфигурации
```

**Решение:**
```bash
# Проверьте конфиг:
xray run -c /usr/local/etc/xray/config.json -test

# Восстановите из резервной копии:
cp /usr/local/etc/xray/backups/config.json.backup.* \
   /usr/local/etc/xray/config.json

# Перезапустите:
systemctl restart xray
```

### Ошибка 4: "Порт 443 занят"

**Проблема:**
```
LISTEN 0 128 0.0.0.0:443 0.0.0.0:* - процесс уже слушает
```

**Решение:**
```bash
# Найдите что занимает порт:
lsof -i :443
# или
ss -tlnp | grep 443

# Убейте процесс (если можно):
fuser -k 443/tcp

# Или смените порт в /usr/local/etc/xray/config.json (не рекомендуется)
```

### Ошибка 5: "Бот не отвечает"

**Проблема:** Бот не появился в Telegram или не отвечает

**Решение:**
```bash
# Проверьте статус:
systemctl status xray_bot

# Посмотрите логи:
journalctl -u xray_bot -n 50

# Проверьте TOKEN в боте:
grep TOKEN_PLACEHOLDER /usr/local/bin/xray_bot.py

# Перезапустите:
systemctl restart xray_bot
```

### Ошибка 6: "Уведомления не приходят"

**Проблема:** Бот не присылает уведомления в Telegram

**Решение:**
```bash
# Проверьте права:
ls -la /usr/local/etc/xray/bot_token.txt
# Должно быть: -rw------- 1 root root

# Если не так - исправьте:
sudo chown root:root /usr/local/etc/xray/bot_token.txt
sudo chmod 600 /usr/local/etc/xray/bot_token.txt

# Проверьте содержимое:
sudo cat /usr/local/etc/xray/bot_token.txt
# Не должно быть пусто

# Отправьте тестовое уведомление:
TOKEN=$(cat /usr/local/etc/xray/bot_token.txt)
CHAT_ID=$(cat /usr/local/etc/xray/.chatid)
curl -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
  -d chat_id="$CHAT_ID" \
  -d text="Test"
```

### Ошибка 7: "Нет подключения"

**Проблема:** Не можете подключиться к VPN

**Решение:**
```bash
# Проверьте статус Xray:
systemctl status xray

# Проверьте слушает ли порт:
ss -tlnp | grep 443

# Проверьте логи:
tail -f /var/log/xray/error.log

# Проверьте доступность маскировки:
curl -I https://github.com

# Проверьте конфиг:
xray run -c /usr/local/etc/xray/config.json -test

# Проверьте пользователя:
listusers
```

## 🎯 Что дальше?

После успешной установки:

1. **Прочитайте**: `XRAY-Admin-Guide.md` - полное руководство
2. **Добавьте пользователей**: Используйте `/newuser` через бота
3. **Протестируйте**: Подключитесь и проверьте скорость
4. **Настройте**: Смените маскировку, добавьте больше пользователей
5. **Мониторьте**: Регулярно проверяйте логи и статус

## 💬 Поддержка

Если что-то не работает:

1. Запустите: `xray-diagnostics.sh`
2. Сохраните вывод
3. Посмотрите логи: `journalctl -u xray -n 100`
4. Создайте Issue в репозитории с информацией

---

**Готово!** Ваш VPN сервер установлен и готов к работе! 🎉

Для управления используйте Telegram бота или SSH команды. Удачи! 🚀
